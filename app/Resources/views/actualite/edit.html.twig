{% extends 'layoutCrud.html.twig' %}

{% block body %}
    <div class="wrapper style3">
    <div class="inner">
    <h1>Actualite modification</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    <ul  id="menu_actions">
        <li>
            <a href="{{ path('actualite_index') }}"><button type="submit">Retourner</button></a>
        </li>
         
    </ul>
    </div></div>
{% endblock %}
