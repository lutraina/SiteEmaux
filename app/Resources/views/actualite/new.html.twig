{% extends 'layoutCrud.html.twig' %}

{% block body %}
    <div class="wrapper style3">
    <div class="inner">
    <h1>Ajout actualité</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajout" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('actualite_index') }}">Retourner</a>
        </li>
    </ul>
    </div></div>
{% endblock %}
