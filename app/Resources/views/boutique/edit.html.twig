{% extends 'layoutCrud.html.twig' %}

{% block body %}
    <div class="wrapper style3">
    <div class="inner">
    <h1>Boutique edit</h1>

     <div>Image actuelle :</div>
    <div class="top-10"><img class="crud_image top-10" src="{{ asset('uploads/'~boutique.image) }}" alt="" width="300"/></div>

 		<form action="{{ path('boutique_update', { 'id': boutique.id }) }}" method="post" {{ form_enctype(edit_form) }}>
            <input type="hidden" name="_method" value="post" />
            {{ form_widget(edit_form) }}
            <p>
                <button type="submit">Modifier</button>
            </p>
        </form>
    <ul id="menu_actions">
        <li>
            <a href="{{ path('boutiqueAll_index') }}"><< Retourner</a>
        </li>
        <!--<li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>-->
    </ul>
            </div></div>
{% endblock %}
